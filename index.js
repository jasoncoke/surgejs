#!/usr/bin/env node

require('./middleware/commander');
// const sftp = require('./middleware/sftp');
// const sshConfig = require('./middleware/ssh');

// const a = new sshConfig();
// a.saveConfigs()